[package]
name = "chela-sss"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shamir's Secret Sharing over GF(2^8): split a byte string into N shares with threshold M, reconstruct from any M shares via Lagrange interpolation."

[dependencies]
chela-field = { path = "../chela-field" }
chela-primitives = { path = "../chela-primitives" }

[lints]
workspace = true
