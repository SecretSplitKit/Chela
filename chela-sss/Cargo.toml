[package]
name = "chela-sss"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shamir's Secret Sharing engine generic over any `chela_field::Field`. Splits a byte string into N shares with threshold M, and reconstructs the byte string from any M of those shares via constant-time Lagrange interpolation."

[dependencies]
chela-field = { path = "../chela-field" }
chela-primitives = { path = "../chela-primitives" }

[lints]
workspace = true
