# Local-dev toolchain. CI and release builds pin to an exact version (see .github/workflows/);
# this file only ensures contributors have rustfmt, clippy, and the wasm target installed.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
profile = "minimal"
